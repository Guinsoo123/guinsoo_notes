# 示例目录说明

`example` 目录的说明已并入上层主文档：

- `../README.md`

请在 `mac-robotics-dev-setup` 根目录执行示例命令，例如：

```bash
cd /Users/guinsoo/guinsoo_notes/mac-robotics-dev-setup
python example/scripts/open3d_quickstart.py
```
